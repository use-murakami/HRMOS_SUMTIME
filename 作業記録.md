# 勤怠・工数突き合わせ自動通知システム — 作業記録

## 基本情報

| 項目 | 内容 |
|---|---|
| プロジェクト名 | 勤怠・工数突き合わせ自動通知システム |
| 記録日 | 2026-04-14（最終更新） |
| 開発手法 | 仕様駆動開発（Spec-Driven Development） |

---

## 1. 要件ヒアリング結果

以下の要件が会話を通じて確定した。

### 1.1 基本要件

| 項目 | 決定内容 |
|---|---|
| 目的 | HRMOS勤怠の勤怠時間と自社工数管理システムの稼働実績の差異検出 |
| 実行頻度 | 週1回（毎週月曜 9:00 JST） |
| インフラ | Google Cloud Platform（GCP） |
| 対象人数 | 約30名 |
| 通知先 | 差異がある本人にも通知（+ 管理者はチャンネルで一覧確認） |

### 1.2 突き合わせ仕様

| 項目 | 決定内容 |
|---|---|
| 突き合わせキー | 社員番号（両システム共通） |
| 差異の閾値 | 30分以上 |
| 集計単位 | 社員ごと・週単位 |

### 1.3 通知仕様

| 項目 | 決定内容 |
|---|---|
| 通知手段 | Microsoft Teams（Power Automate Workflows Webhook） |
| 通知方式 | 差異のある社員ごとに1件ずつAdaptive Cardを投稿 |
| メンション | 各カード内で本人を `<at>` タグでメンション |

---

## 2. 技術調査・確認事項

### 2.1 GCP 無料枠の確認

GCP の各サービスが無料枠内で運用可能であることを確認した。

| サービス | 無料枠 | 想定使用量 | 判定 |
|---|---|---|---|
| Cloud Scheduler | 3ジョブ/アカウント（登録数） | 1ジョブ | ✅ 範囲内 |
| Cloud Functions 呼び出し | 200万回/月 | 約4回/月 | ✅ 範囲内 |
| Cloud Functions 実行時間 | 400,000 GB秒/月 | 約0.5 GB秒/月 | ✅ 範囲内 |
| BigQuery クエリ | 1 TB/月 | 数MB/月 | ✅ 範囲内 |
| BigQuery ストレージ | 10 GB | 数KB〜数MB | ✅ 範囲内 |

**注意事項：**
- Cloud Schedulerの「3ジョブ」は実行回数ではなく登録ジョブ数。週1実行でも登録は1ジョブなので無料枠内。
- Cloud Functionsデプロイ時にCloud Build / Artifact Registryが使われるが、頻繁なデプロイでなければ実質無料。
- 念のためGCPの請求アラートを$1程度に設定することを推奨。

### 2.2 Teams Incoming Webhook の調査

- 従来の Office 365 コネクタ（Incoming Webhook）はMicrosoftが廃止を進めている。
- 廃止期限は2026年4月30日まで延長されている。
- **新規構築では Power Automate Workflows の Webhook トリガーを使用すべき。**
- Workflows でも URL に POST するだけで通知可能。URLの形式が異なるのみ。

### 2.3 HRMOS勤怠 API の調査（2026-03-25）

- 公式ドキュメント: https://ieyasu.co/docs/api.html
- OpenAPI定義: https://ieyasu.co/docs/openapi.json
- 認証方式: APIキー → Basic認証でトークン取得（有効期限24時間、延長API あり）
- 使用エンドポイント: `GET /work_outputs/monthly/{month}`
- 必要なフィールドはすべて取得可能であることを確認（社員番号=`number`、勤務日=`day`、出勤=`start_at`、退勤=`end_at`、実労働時間=`actual_working_hours`）
- 休憩時間は `break_1_start_at`/`break_1_end_at` 等の差分から算出が必要
- レート制限: 過去60秒累計で制限（具体的数値は非公開、超過時 HTTP 429）
- APIキー取得には有料プランが必要（管理者権限でシステム設定から発行）

### 2.4 工数管理システムのデータ取得方式（2026-03-25）

- API経由ではなくDB直接アクセスで取得する方針に決定
- テーブル構造・接続情報は別途確認予定

### 2.5 Teams メンション機能の調査

- Adaptive Card 内で `<at>` タグと `msteams.entities` を使うことで特定ユーザーへのメンションが可能。
- メンションには Azure AD の UPN（メールアドレス）または Object ID が必要。
- 1メッセージあたりのメンション上限は20ユーザー。1人1カード方式なら制約に抵触しない。

---

## 3. 設計

### 3.1 システム構成図

以下の構成で合意した。

```
Cloud Scheduler → Cloud Functions → HRMOS勤怠 API
                                  → 工数管理システム DB
                                  → BigQuery（ログ蓄積・任意）
                                  → Teams Webhook（通知）
```

### 3.2 通知方式の決定

2パターンを検討し、パターン1を採用。

- ✅ **パターン1（採用）：** 差異がある社員ごとに個別のAdaptive Cardを投稿し、各カードで本人をメンション
- ❌ パターン2：全員の差異を1枚のカードにまとめて複数人を同時メンション

採用理由：本人にとって自分の差異だけ見えてわかりやすい。

### 3.3 アカウント構成の決定

小規模な業務改善ツールとして個人主導で開始する方針を決定。

| 環境 | アカウント種別 |
|---|---|
| GCP | 個人 Google アカウント |
| Microsoft Teams | 社内アカウント |

**認識しているリスク：**

- GCPアカウント所有者の退職・閉鎖時にシステム停止
- 請求責任が個人に帰属
- 社内APIキーを個人GCPに保存する点のセキュリティ確認が必要
- Teams Webhook は作成ユーザーに紐づくため、共同所有者の設定が必要

**方針：** 運用が定着した段階で会社管理のGCPアカウントへ移行する。

---

## 4. 成果物

### 4.1 作成済み

| 成果物 | ファイル名 | 状態 |
|---|---|---|
| システム仕様書 | `勤怠工数突き合わせシステム_仕様書.md` | HRMOS API連携仕様を詳細化済み（セクション3.2） |
| 作業記録 | `作業記録.md`（本文書） | 作成済み |

### 4.2 未着手

| 成果物 | 予定フェーズ |
|---|---|
| API仕様マッピング定義書（工数管理DB側） | Phase 2 |
| Cloud Functions コード | Phase 3〜4 |
| テストコード | Phase 3〜4 |
| Terraform or gcloud スクリプト | Phase 6 |
| 運用手順書 | Phase 7 |

---

## 5. 未決事項

| # | 項目 | 説明 | ステータス |
|---|---|---|---|
| 1 | HRMOS勤怠 API の認証方式・エンドポイント | トークン認証、`/work_outputs/monthly/{month}` を使用。フィールドマッピング確定 | **解決済み** |
| 2 | 工数管理システムのデータ取得方式 | DB直接アクセスに変更。テーブル構造・接続情報の確認が必要 | 方針決定済み・詳細未着手 |
| 3 | 社員マスタの管理方法 | HRMOS `/users` APIで取得可能と判明（選択肢B推奨）。要決定 | 未決定 |
| 4 | Teams 通知先チャンネル | チャンネル名・Webhook URL の準備 | 未準備 |
| 5 | 管理者の定義 | サマリー通知を受け取る管理者の範囲 | 未決定 |
| 6 | Cloud Functions のランタイム | Python 3.12 を想定 | 要確認 |
| 7 | 実行時刻の最終確認 | 月曜 9:00 JST で問題ないか | 要確認 |
| 8 | 社内IT管理者への確認 | 個人GCPへの社内APIキー保存、Teams Webhook利用の承認 | 未着手 |
| 9 | Teams Webhook の共同所有者設定 | Workflow作成者の退職リスクへの対策 | 未着手 |

---

## 6. 作業ログ

### 2026-03-31 — HRMOS API・SUMTIME DB実機確認 + 仕様確定

**実施内容:**

1. **HRMOS API 実機確認**
   - `tools/verify_api.py` を作成・実行
   - 認証方式確定: Secret Keyは既にBase64済み、`Authorization: Basic {secret_key}` で直接使用
   - `actual_working_hours`（"H:MM"形式）、`segment_display_title`（日本語文字列）、`email`（全ユーザーに存在）を確認

2. **SUMTIME DB 実機確認**
   - `tools/verify_db.py` を作成・実行（SSHトンネル経由PostgreSQL）
   - 全48テーブルのスキーマ・サンプルデータを `tools/db_schema/` に保存
   - **重要発見: SUMTIMEに社員番号フィールドなし** → 突き合わせキーをメールアドレスに変更

3. **仕様確定（ユーザーとの対話で決定）**

| 項目 | 旧仕様 | 新仕様 |
|---|---|---|
| 突き合わせキー | 社員番号 | **メールアドレス（email）** |
| 突き合わせ単位 | 週単位 | **日単位のみ** |
| 閾値 | 30分 | **15分/日** |
| 対象範囲 | 前週月〜日 | **過去2週間** |
| 実行スケジュール | 週1回 月曜9:00 | **月〜金 毎日9:00** |
| 通知方式 | 個人カード + 管理者サマリー | **個人カードのみ**（差異日のみ表示、毎日リマインド） |
| 通知動作 | — | **差異が修正されるまで毎日再通知** |
| エラー通知 | 未定 | **管理者（村上）にTeams通知** |
| 管理UI | なし | **Cloud Run + Google認証**（設定・検証用対象者選択・手動実行・結果プレビュー） |
| 認証情報管理 | 未定 | **Secret Manager（9シークレット）** |
| DB接続 | 未定 | **SSHトンネル + パスワード認証** |
| BigQueryログ | 任意 | **初期リリースでは不要** |
| SUMTIME未登録者(5名) | — | **通知対象外** |

**成果物:**
- `tools/verify_api.py` — HRMOS API検証スクリプト
- `tools/verify_db.py` — SUMTIME DB検証スクリプト
- `tools/db_schema/` — 全48テーブルのスキーマ情報（JSON）
- `README.md` — プロジェクト概要

---

## 7. 作業ログ（追記）

### 2026-04-14 — 仕様書 v0.2 確定 + 仕様レビュー・補完

**実施内容:**

1. **仕様書への確定仕様反映（v0.1 → v0.2）**
   - 2026-03-31確定の仕様を仕様書に全面反映
   - 突き合わせキー（email）・日単位・15分閾値・過去2週間・月〜金9:00

2. **仕様レビュー — 不整合の修正（4件）**
   - 「週1回の実行」→「毎日の実行」
   - 社員マスタテーブル定義のemail化
   - 外部サービス接続方式の具体化
   - BigQuery関連記述の将来機能化

3. **仕様レビュー — 漏れの補完（7件）**
   - Cloud Functions実行仕様: **第2世代**、タイムアウト300秒、メモリ256MB
   - タイムゾーン: 両システムともJST、`TZ=Asia/Tokyo` を環境変数設定
   - 祝日: `jpholiday` ライブラリで判定、祝日はスキップ
   - 部分障害: 片方失敗で全体中止、管理者エラー通知
   - 管理UIとCloud Functionsの連携: HTTPトリガー、プレビューモード

4. **Teams通知方式の変更**
   - 当初: Power Automate Workflows Webhook（チャンネル投稿）
   - → Power Automate フロー（個人DM）を検討
   - → **Power Automate の HTTPトリガーは Premium（有料 ~$15/月）が必要** と判明
   - → **Microsoft Graph API 直接呼び出し（無料）に決定**
   - Azure AD アプリ登録 + `Chat.Create`/`ChatMessage.Send` 権限
   - Secret Manager: 9 → **11シークレット**に増加（Azure AD 3件追加）

5. **別システムの知見を反映**
   - Teams Incoming Webhook の28KBサイズ制限（Graph APIでは適用外）
   - HTTP 200でエラー文字列を返すサイレント失敗パターンへの対策
   - Adaptive Cardデザイン制約（カスタムカラー非対応）の注意書き
   - Adaptive Card バージョンを1.4→1.5に修正（Table要素対応）

### 2026-04-18 — Graph API Teams DM送信 検証スクリプト作成

**実施内容:**

1. **検証スクリプト作成** `tools/verify_teams_dm.py`
   - Azure AD Client Credentials → Graph API でTeams DM送信を7ステップで検証
   - Step 1: アクセストークン取得
   - Step 2: 対象ユーザーのオブジェクトID取得
   - Step 3: アプリのサービスプリンシパルID取得
   - Step 4: 1:1チャット作成
   - Step 5: テキストメッセージ送信
   - Step 6: Adaptive Card送信（本番想定フォーマット）
   - Step 7: 結果サマリー表示

2. **実行手順書作成** `tools/README_verify_teams_dm.md`
   - Azure Portalでのアプリ登録手順（スクリーンショットなし・テキストのみ）
   - 必要な権限: `Chat.Create`, `ChatMessage.Send`, `User.Read.All`
   - よくあるエラーと対処法（403/401/404）
   - 検証後の対応フロー

3. **確認事項**
   - グローバル管理者であれば Azure ADのアプリ登録・管理者同意まで自己完結可能と確認
   - スクリプトはファイル作成のみ。Azure ADへの操作は未着手

**成果物:**
- `tools/verify_teams_dm.py`
- `tools/README_verify_teams_dm.md`

### 2026-04-20 — HRMOS×SUMTIME 実績乖離調査スクリプト群の作成

**背景:** 仕様製造を一旦中断し、現時点のHRMOSとSUMTIMEの実績に乖離があるかを手動で調査するため。

**実施内容:**

1. **HRMOS 人別・月別 稼働時間集計スクリプト** `tools/hrmos_monthly_hours.py`
   - HRMOSのAPIから人別・月別の実働時間合計を取得・表示
   - 出力: 実働時間（H:MM）テーブル＋勤務日数テーブル
   - **仕様確認事項（調査中に判明）:**
     - `actual_working_hours` が正しい実働時間フィールド
     - セグメント名ホワイトリストでは「出勤（月例）」が漏れる → `actual_working_hours > 0` でフィルターする方式に変更
     - 「出勤（月例）」は集計除外（土曜月例出勤などは工数突き合わせ対象外）
     - HRMOS実働時間は日別に15分切り捨て（SUMTIMEが15分単位入力のため）

2. **SUMTIME 人別・月別 工数集計スクリプト** `tools/sumtime_monthly_hours.py`
   - SUMTIMEのworking_achievementsを人別・月別に集計
   - SSHトンネル経由でPostgreSQLに接続
   - 出力: 工数合計（H:MM）テーブル＋工数入力日数テーブル

3. **月別突き合わせスクリプト** `tools/check_discrepancy.py`（既存ファイルを全面改訂）
   - HRMOS×SUMTIMEを人別・月別で突き合わせ
   - 突き合わせキー: HRMOSの `login_id`（=email）← SUMTIMEの `email`
   - 15分以上の差異に `◀` マーク
   - SUMTIME未登録ユーザーを末尾に表示

4. **日別突き合わせスクリプト** `tools/check_discrepancy_daily.py`
   - 特定ユーザー・特定月を日別に突き合わせ
   - 日付・曜日・勤務区分・HRMOS実働・SUMTIME工数・差異を1行表示
   - 月合計行・差異件数サマリーあり

**スクリプト一覧まとめ:**

| ファイル | 用途 | 主な引数 |
|---|---|---|
| `tools/hrmos_monthly_hours.py` | HRMOSの人別・月別実働時間 | `--company-url` `--secret-key` `--months` |
| `tools/sumtime_monthly_hours.py` | SUMTIMEの人別・月別工数 | SSH/DB接続情報 `--months` |
| `tools/check_discrepancy.py` | 月別突き合わせ（全員） | HRMOS + SSH/DB接続情報 `--months` |
| `tools/check_discrepancy_daily.py` | 日別突き合わせ（特定ユーザー・月） | HRMOS + SSH/DB接続情報 `--login-id` `--month` |

**共通仕様（全スクリプト):**
- HRMOS実働時間: `actual_working_hours` フィールド、日別15分切り捨て後に月集計
- 「月例」を含む勤務区分は除外
- 突き合わせキー: メールアドレス（HRMOSは `email` フィールド＝ログインID）

---

### 2026-04-20（追記）— Teams通知断念・メール通知への変更 + Azure AD アプリ設定完了

**実施内容:**

1. **Graph API Teams DM送信の実機検証**
   - Azure ADアプリ「Teamsチャット通知検証」を登録（Chat.Create / Chat.ReadWrite.All / User.Read.All）
   - `tools/verify_teams_dm.py` を実行 → Step 4（チャット作成）で HTTP 403
   - **原因判明: サービスプリンシパルは1:1チャットのメンバーになれない（Teams仕様上の制約）**
   - グループチャット（chatType: "group"）に変更しても同様のエラー

2. **代替方式の検討・選定**
   - Teamsボット方式: Azureサブスクリプションが必要 → 現環境では取得していないため断念
   - 専用通知アカウント: 月額ライセンスが必要なため断念
   - **メール通知（Graph API Mail.Send）に決定** → 無料・確実・実績ある方法

3. **Azure ADアプリの権限整理**
   - 不要権限（Chat.Create / Chat.ReadWrite.All / User.Read 委任 / User.Read.All）を削除
   - `Mail.Send`（アプリケーション権限）のみに整理・管理者同意付与

4. **メール送信検証スクリプト作成・実行**
   - `tools/verify_email.py` を新規作成
   - テキストメール・HTML表形式メールの2件送信を確認 ✓
   - 共有メールボックスを送信元にする方針も決定（次回作業）

5. **仕様書 v0.2 → v0.3 更新**
   - 通知方式: Teams DM → メール（Graph API Mail.Send）
   - 付録B追加: Teams通知断念の経緯・検証結果・将来の移行方針

**成果物:**
- `tools/verify_email.py` — Graph API メール送信検証スクリプト
- `勤怠工数突き合わせシステム_仕様書.md` v0.3（通知方式変更・付録B追加）

**クライアントシークレット有効期限:** 2026-10-17（180日）  
→ **2026-07-17頃にカレンダーリマインダー設定推奨**

---

### 2026-04-24 — GCPインフラ構築・社内PCスクリプト作成・稼働検証

**実施内容:**

1. **共有メールボックス作成・動作確認**
   - Microsoft 365管理センターで `kintai-notice@use-eng.co.jp` を作成
   - `tools/verify_email.py` で送信元を共有メールボックスに変更して実行 → 2通受信確認 ✓
   - ライセンス不要・追加コストなしで送信元として使用可能なことを確認

2. **GCPプロジェクト作成**
   - プロジェクト名: `kintai-kosu-notification`
   - 個人Googleアカウントで作成

3. **請求アラート設定**
   - 予算: 1円（少額でも課金発生を即検知するため）
   - しきい値: 50% / 90% / 100%

4. **必要なAPIの有効化**（5つ）
   - Cloud Functions API / Cloud Scheduler API / Cloud Run Admin API
   - Secret Manager API / Cloud Build API

5. **Secret Manager へのシークレット登録**（11件）
   - HRMOS関連: `hrmos-company-url`, `hrmos-secret-key`
   - SUMTIME SSH関連: `sumtime-ssh-host`, `sumtime-ssh-user`, `sumtime-ssh-password`
   - SUMTIME DB関連: `sumtime-db-host`, `sumtime-db-name`, `sumtime-db-user`, `sumtime-db-password`
   - Azure AD関連: `azure-tenant-id`, `azure-client-id`, `azure-client-secret`

6. **Cloud Storage バケット作成**
   - バケット名: `kintai-kosu-sumtime-data`（マルチリージョン: asia）

7. **ハイブリッドアーキテクチャの設計・決定**
   - **課題**: SUMTIMEサーバー（192.168.1.17）がプライベートIPのためGCPから到達不可
   - **解決策**: 社内PCがSUMTIMEデータを取得→Cloud Storageへアップロード→Cloud Functionsで処理
   - 社内PCはSecret ManagerからSSH/DB認証情報を取得（ローカルに保存しない設計）

8. **GCPサービスアカウント作成（社内PC用）**
   - サービスアカウント名: `office-pc-runner`
   - 付与した権限:
     - `roles/secretmanager.secretAccessor`（Secret Manager読み取り）
     - `roles/storage.objectCreator`（Cloud Storage書き込み）
     - `roles/storage.objectViewer`（Cloud Storage読み取り）
   - キーファイル（`office-pc-key.json`）を社内PCに配置
   - `.gitignore` に追加済み（`*-key.json`）

9. **社内PCスクリプト作成・実行検証**
   - `tools/fetch_sumtime_to_gcs.py` を新規作成
   - 処理フロー: Secret Manager認証情報取得 → SSHトンネル経由でSUMTIME DB接続 → 過去2週間データ取得 → GCS upload
   - 実行結果: **12名・118件のレコードを取得・アップロード成功** ✓
   - アップロード先: `gs://kintai-kosu-sumtime-data/sumtime_data/2026-04-24/sumtime_2026-04-24.json`

10. **Cloud Shell からのメール送信検証**
    - GCP Cloud Shell 上で `tools/verify_email.py` を実行
    - Cloud Functions実行環境（GCP）からもメール送信が正常に動作することを確認 ✓

11. **Cloud Functions デフォルトサービスアカウントへの権限付与**
    - `roles/editor`（既存）
    - `roles/secretmanager.secretAccessor`
    - `roles/storage.objectViewer`
    - Cloud Functions から Secret Manager・Cloud Storage にアクセスできる状態を確認

**成果物:**
- `tools/fetch_sumtime_to_gcs.py` — 社内PC用 SUMTIMEデータ取得→GCSアップロードスクリプト
- `office-pc-key.json` — 社内PC用GCPサービスアカウントキー（git管理外）
- GCPインフラ（Secret Manager 11件・Cloud Storage バケット・サービスアカウント）構築完了

**動作確認済み:**
- ✅ 社内PC → Secret Manager → SUMTIME DB (SSH) → Cloud Storage の一気通貫フロー
- ✅ GCP Cloud Shell → Graph API → メール送信

---

## 8. 次のアクション

### 8.1 実装前準備（優先度: 高）

| # | アクション | 備考 |
|---|---|---|
| 1 | ~~共有メールボックス作成~~ | ✅ 完了（kintai-notice@use-eng.co.jp） |
| 2 | ~~GCPプロジェクト作成~~ | ✅ 完了（kintai-kosu-notification） |
| 3 | ~~Secret Manager 登録~~ | ✅ 完了（11シークレット） |
| 4 | ~~Cloud Storage バケット作成~~ | ✅ 完了（kintai-kosu-sumtime-data） |
| 5 | ~~社内PCスクリプト作成・検証~~ | ✅ 完了（fetch_sumtime_to_gcs.py） |
| 6 | Cloud Functions 疎通確認 | 最小テスト関数のデプロイ・Secret Manager/GCS アクセス確認 |
| 7 | 社内IT管理者への確認 | 個人GCPへのAPIキー保存、Azure AD アプリ権限の承認（事後報告でも可） |

---

### 2026-05-01 — Cloud Functions 疎通確認 + メール通知仕様詳細化

**実施内容:**

1. **Cloud Functions 最小テスト関数デプロイ・動作確認**
   - Cloud Shell でテスト関数を作成・デプロイ（asia-northeast1, Python 3.12, 第2世代）
   - `{"secret_manager": "✓ 成功", "cloud_storage": "✓ 成功"}` の結果を確認
   - テスト関数は確認後に削除

2. **検証モード・本番モードの仕様決定**
   - Cloud Functions の呼び出しパラメータで切り替える方式に決定
   - 管理UIは将来機能として棚上げ（初期リリース対象外）

   | 項目 | 検証モード | 本番モード |
   |---|---|---|
   | トリガー | curlコマンド（手動） | Cloud Scheduler（自動） |
   | 対象ユーザー | パラメータ指定の1名 | SUMTIME登録済み全員 |
   | メール送信先 | 対象ユーザー本人 | 各ユーザー本人 |
   | パラメータ例 | `{"mode": "test", "target_email": "xxx@use-eng.co.jp"}` | `{"mode": "production"}` |

3. **メール通知仕様の詳細化（途中）**
   - 差異の表示形式（+/-符号・赤字統一）について検討開始
   - 管理者サマリー通知の要否について検討中（次回継続）

**動作確認済み:**
- ✅ Cloud Functions → Secret Manager アクセス
- ✅ Cloud Functions → Cloud Storage アクセス

---

## 8. 次のアクション

### 8.1 実装前準備（優先度: 高）

| # | アクション | 備考 |
|---|---|---|
| 1 | ~~共有メールボックス作成~~ | ✅ 完了（kintai-notice@use-eng.co.jp） |
| 2 | ~~GCPプロジェクト作成~~ | ✅ 完了（kintai-kosu-notification） |
| 3 | ~~Secret Manager 登録~~ | ✅ 完了（11シークレット） |
| 4 | ~~Cloud Storage バケット作成~~ | ✅ 完了（kintai-kosu-sumtime-data） |
| 5 | ~~社内PCスクリプト作成・検証~~ | ✅ 完了（fetch_sumtime_to_gcs.py） |
| 6 | ~~Cloud Functions 疎通確認~~ | ✅ 完了（Secret Manager・GCS アクセス確認） |
| 7 | 社内IT管理者への確認 | 個人GCPへのAPIキー保存、Azure AD アプリ権限の承認（事後報告でも可） |

### 8.2 次回作業（優先度: 高）

1. **メール通知仕様の続き**
   - 差異の表示形式（+/-符号・色・工数未入力の表示）の確定
   - 管理者サマリー通知の要否
   - 「工数あり・勤怠なし」ケースの通知有無

2. **仕様書 v0.3 → v0.4 更新**
   - 検証モード・本番モードの仕様を追記
   - 管理UIを将来機能に変更
   - Teams関連記述の残り（セクション3.4, 7.2, 7.3, 10.2, 10.3, 11.1, 11.3, 12）をメールに修正
   - セクション8.1「全9シークレット」→「全11シークレット」修正
   - セクション5.1 送信元アドレスを `kintai-notice@use-eng.co.jp` に更新
   - アーキテクチャ図をハイブリッド構成（社内PC + GCS）に更新
   - セクション13 未決事項4（Azure AD権限変更）を解決済みに更新

3. **Phase 3（Cloud Functions 本実装）開始**
